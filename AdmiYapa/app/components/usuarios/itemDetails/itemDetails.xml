<Page xmlns="http://schemas.nativescript.org/tns.xsd" xmlns:widgets="utils/widgets" navigatedTo="navigatedTo" id="usuariosModelDetailsScreen" cssClass="screen">
    <Page.actionBar>
        <widgets:action-bar />
    </Page.actionBar>

    <ScrollView>
        <StackLayout>

            <GridLayout rows="auto, *">
                <GridLayout rows="auto" columns="*, *">


                </GridLayout>
                <StackLayout row="1">

                    <Label text="Tienda 1" />
                    
                    <Label text="{{ 'DNI: ' + Username }}" />
                    
                    <Label text="{{ 'Teléfono: ' + DisplayName }}" />

                    <Label text="{{ 'Email: ' + Email }}" />
                    
                    <Label text="" id="fecha" textWrap="true" />

                    <!-- additional view content -->
                </StackLayout>
            </GridLayout>

        </StackLayout>
    </ScrollView>
</Page>